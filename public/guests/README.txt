Drop each invitee's photo here named after the "img" key in src/data/guests.json.

Example: guests.json has  "adi": { ..., "img": "adi" }
  -> put the file:  public/guests/adi.jpg

Square images look best (they're shown in a circular silver frame).
If a photo is missing, the page gracefully shows the guest's initial instead.
